# Public claims: corrections prepared for founder review

Prepared 2026-09-02 inside the feasibility package. Nothing outside `feasibility/` was edited. This document lists every public numeric or capability claim found on Telo's public surfaces, the stored artifact each one should trace to, and the wording that is true under the stored artifacts and under the feasibility package's independent re-run. Register: `CLAIMS_REGISTER.csv` (C001-C024). Paths are relative to `~/telo/` unless they start with `feasibility/`.

## 0. What is actually public, and where the numbers live

| Surface | Public state (checked 2026-09-02) |
|---|---|
| GitHub `rikhinkavuru/Telo` | public (`gh repo view`: isPrivate false, updated 2026-09-01); HEAD = origin/main = c3b31b9 (2026-07-26). Root `README.md` and `telo-web/` (40 tracked files, working tree unmodified) are therefore public as they stand. `CLAUDE.md`, `docs/startup-outline.md`, `funding/emergent-ventures-proposal.md`, `research/conformal/README.md` and `research/conformal/artifact/release-layer.html` are modified and uncommitted, so the public copies are the older HEAD versions. |
| Website | Next.js app in `telo-web/`, Vercel project `telo-web` (`telo-web/.vercel/project.json`). `src/app/layout.jsx:4` sets `metadataBase` to https://telo.ai; live deployment not verified in this pass. Copy lives in `src/data/research.js`, `src/data/content.js`, `src/app/research/page.jsx`, `src/app/layout.jsx`, `src/components/sections/Engagements.jsx`. |
| `research/conformal/README.md` | public HEAD version is 127 lines (corn plus tablet drift tables, "654", "~1%"); working-tree version is 707 lines and carries the retractions (86.9% at a proved 90%, 0.7%, reference-noise argument withdrawn). The corrections exist but are not published. |
| `research/conformal/artifact/release-layer.html` | public HEAD version differs from the working tree (HEAD: 0 occurrences of "89%", 0 of "86.9"; working tree: 6 lines (7 occurrences) and 2). Only counts were checked. |
| `funding/deck/telo-deck.{html,pdf}`, `funding/deck-prompt.md` | tracked; deck already says furosemide and $696.7M; its proof bars are the corn triple 9 / 99 / 3. |
| `funding/emergent-ventures-proposal.md` | tracked, modified in working tree; grant text (external if submitted). Says 87% at 0.3% at a proved 90% (matches RRCM) and "up to $812 million". |
| `paper/release-layer-manuscript.md` | not submitted, no preprint posted (`paper/SUBMISSION.md` item 1; root `CLAUDE.md:74-77`). Uses 615 units, 86.9%, 71.5%, 0.7%. Not a public surface today. |

Stored artifacts used: `research/conformal/results/*.json` (tablet_summary, benchmark_summary, guarantee_summary, frontier_summary, bestmodel_summary, sota_summary, operating_summary, pointmodel_summary, risk_summary, multisite_summary, summary [corn]); `feasibility/results/release_assurance/ra_*/metrics.json` and `model_card.md`; `feasibility/docs/methods/release_benchmark.md`; `feasibility/docs/audits/03_conformal_reproduction_audit.md`; `regulatory/release-constraints.md`; `feasibility/data/raw_snapshots/S27/2026-09-01/usaspending_phlow.json`.

## 1. Claim table

Change classes: (a) working-tree change the agent may make now (only inside `feasibility/`); (b) needs founder approval (any file outside `feasibility/`); (c) public-history remediation; (d) website correction and redeploy; (e) claim to remove; (f) claim to qualify. Vocabulary note used throughout: "mis-release exposure" in the tablet and corn scripts is an interval-miss rate (1 minus coverage, or release rate times 1 minus coverage-on-released), not an out-of-specification release rate; the release-yield benchmark's "false release" is the out-of-specification rate. The two are different quantities (03 audit section 4).

| id | Current text (file:line) | All surfaces | Stored value (artifact, key) | Verdict | Replacement text | Class |
|---|---|---|---|---|---|---|
| C001 | "654 pharmaceutical tablets measured on two spectrometers" (`telo-web/src/app/research/page.jsx:187`) | `telo-web/src/data/research.js:9`; `research/conformal/data_tablet.py:3`; public `research/conformal/README.md@HEAD:78`; working-tree README:38 notes the 654/655 discrepancy as unresolved | `data/tablet.mat` holds 155 + 460 + 40 = 655 (03 audit section 2); experiments index 155 calibration + 460 test = 615 (`multisite_summary.json benchmark_exchangeability.calibration.n`=155, `.test.n`=460); Eigenvector's page says 654 | contradicted (count) | "615 tablets (155 calibration, 460 test) from the IDRC 2002 NIR shootout, each measured on two spectrometers; the distribution ships a further 40-tablet validation file that is unused" | d, b, f |
| C002 | "collapses the point model from R² = 0.88 to 0.05" (`page.jsx:204`) | `research.js:61`; README@HEAD:85 | `tablet_summary.json instruments.home.r2`=0.8772, `instruments.shift.r2`=0.0463 (mean over 200 seeds; per-seed range -4.62 to 0.855; bootstrap CI [-0.10, 0.18], 03 audit section 3) | supported with caveat | "mean R² fell from 0.88 to 0.05 on the second instrument (bootstrap 95% CI -0.10 to 0.18 over 200 seeds)" | d, f |
| C003 | "drops naive conformal coverage from 91% to 42%" (`page.jsx:204-205`) | `research.js:62`; README@HEAD:86 | `tablet_summary.json instruments.home.naive_coverage`=0.9142, `shift.naive_coverage`=0.4159 (CI [0.371, 0.460]); feasibility re-run: reproduction mode 0.410 [0.328, 0.493] (`ra_20260902T045127Z/metrics.json metrics.shift.empirical_coverage`), no standardization 0.768 (`ra_20260902T044739Z`), CV components 0.717 (`ra_20260902T044654Z`) | supported for the stated pipeline; implementation-sensitive | "with the repository pipeline (8 PLS components, per-wavelength standardization) nominal-90% coverage fell from 91% to 42% on the second instrument; a standard pipeline without that standardization fell to 72-77%" | d, f |
| C004 | "58% of batches on the changed instrument would ship with a wrong certificate" (`page.jsx:205-206`, Figure 1 caption :209) | `research.js:63` shiftNaive=58, `:127` v:[9,58] | `tablet_summary.json instruments.shift.misrelease_naive`=0.5841 = 1 - 0.4159; denominator 460 x 200 = 92,000 tablet-evaluations | supported as an interval-miss rate | "without a gate, 58% of shifted-instrument predictions fell outside their nominal-90% interval (an interval-miss rate, not an out-of-specification release rate)" | d, f |
| C005 | "Mis-release exposure 9% / 58% -> ~1% with the gate" (public README@HEAD:87) | working-tree README:324 says 0.7% | `tablet_summary.json instruments.shift.misrelease_gated`=0.00739; `shift.release_rate`=0.01097 (177 of 200 seeds released nothing); feasibility reproduction `metrics.shift.unconditional_wrong_release`=0.0058 [0.0007, 0.0137], no standardization 0.046, CV components 0.155 | supported with mandatory caveat; implementation-sensitive | see section 2 wording W1 | b, f |
| C006 | conditional coverage among released 32.6% (internal only) | not on any public surface | `tablet_summary.json instruments.shift.coverage_on_released`=0.3260 over 23 seeds; pooled 308/1009 = 0.305; bootstrap CI [0.17, 0.50] (03 audit section 3, 5,000 resamples over seeds; stored in audit prose only, not in any JSON); feasibility reproduction 0.609 [0.27, 0.88] over 7 of 50 seeds, 0.160 and 0.290 in the other modes | supported with small-denominator caveat | keep internal; if used: "among the few shifted tablets the gate released, coverage was 31-33% in the repository run and 39-84% across re-run modes; the gate does not release at useful quality under shift" | a |
| C007 | "holding mis-release near 3%" (`page.jsx:128-129`, Figure 1 caption :209) | `research.js:63` shiftGated=3, `:126` telo v:[8,3]; deck `funding/deck/telo-deck.html:463-465` PROOF = 9 / 99 / 3 | no tablet key equals 3%: tablet gated = 0.74%. Probable origin: corn protein, changed sensor mp6, gated exposure = `summary.json properties.protein.instruments.mp6.abstain_release_rate` 0.0348 x (1 - `abstain_coverage_on_released` 0.0037) = 0.0347; mp5 gives 0.044 x 1.0 = 0.044; home m5 0.899 x (1 - 0.912) = 0.079 (the "8" in research.js:126). The deck triple 9 / 99 / 3 is the corn set (naive mp5/mp6 coverage 0.014 / 0.011). The website Figure 1 pairs tablet naive numbers (9, 58) with corn gated numbers (8, 3). | contradicted on the website (dataset mix); supported with caveat on the deck (corn mp6, 3.5% rounded down, better of two instruments) | website: "the gate cut interval-miss exposure from 58% to 0.7% by abstaining on 99% of shifted tablets" and make both bars of the drift chart tablet-only (home naive 8.6%, home gated 6.6%, shift naive 58.4%, shift gated 0.7%). Deck: label the bars "corn NIR, protein" and use 4% (mp5) or "3.5-4.4%" | d, e (the 3%), f (deck) |
| C008 | "Beachhead: norepinephrine (lead ...) -> epinephrine (flagship ...)" (root `README.md:7`) | `docs/startup-outline.md:84-85` [V] (public HEAD and working tree); root `README.md:25`; contradicted inside the repo by `research/beachhead-verification.md` (2026-07-16), `docs/roadmap.md:25`, deck html:348, `funding/deck-prompt.md:53`, EV proposal:33 (all say furosemide) | FDA shortage export S01 2026-09-02: norepinephrine 0 rows, furosemide 33 rows since 2020-04-07 (`feasibility/HANDOFF.md` section 2); furosemide shortage status still secondary-source per roadmap | contradicted by the repo's own memo | "Beachhead under revision: the original norepinephrine/epinephrine pick was withdrawn after primary-source checks (2026-07-16); furosemide 10 mg/mL is the current candidate pending a primary-source shortage check; the feasibility study screens presentations from evidence" | b, f |
| C009 | "released in real time by an AI quality layer instead of a two-week lab hold" (root `README.md:3`) | (a) state a duration of about two weeks: root `README.md:3`; root `CLAUDE.md:3` (HEAD identical: "replacing the ~2-week end-of-batch QC hold"); `docs/startup-outline.md:13`; `research.js:28` note "two-week hold", `:47` "Real-time release, no multi-week hold"; deck html:264; `deck-prompt.md:43`; artifact html:164 (its line 296 already concedes ~0 days for injectables). (b) state only that real-time release removes the hold, with no two-week figure: `page.jsx:120-121` ("multi-day quality hold"), `:142-145` ("waits days for the assay", "end-of-batch wait"); EV proposal:29 ("instead of mailing samples away and waiting"). The replacement text applies to both groups | `regulatory/release-constraints.md` section 1: USP <71> sterility >= 14 days runs in parallel from t=0, chemical tests finish in 5-7 days, so chemical RTR saves ~0 days for aseptically filled injectables; section 1 (line 69): even at zero-day sterility a ~7-day floor from environmental monitoring remains; section 1a (lines 105-117): rapid sterility is a vendor claim (~1 business day, line 108) and EM plates still incubate 5-7 days (line 117); feasibility test `test_full_release_assurance_saves_no_days_when_sterility_binds`; `docs/startup-outline.md:41, 206` already record the correction | contradicted by own analysis | "For aseptically filled injectables, chemical real-time release does not shorten the release hold while the 14-day sterility incubation is the critical path; it shortens the hold for oral solid dose. The candidate value for injectables is an assured beyond-use date and distribution radius, which is unvalidated." | b, d, f |
| C010 | "Telo automates the release decision" (`telo-web/src/app/layout.jsx:7`, `src/data/content.js:6`); "certifies pharmaceutical batches in real time" (`page.jsx:17, 126`); "Per-batch release in real time" (`Engagements.jsx:18`) | protocol claim-discipline row `feasibility/protocol/protocol.yaml:497-498`; `os/quality-intelligence/README.md:335` "Advisory. Not a batch release decision. Release remains the authorised person's."; protocol `current_evidence_boundary` (:397) | none: the OS component is advisory by design; the evidence is a methods proof of shift detection and abstention on public spectra | contradicted | protocol wording: "In a public-benchmark methods study, our shift detector abstained under instrument drift; prospective product/site validation is the next step." Site copy: "Telo builds the release-decision support layer and the small plants it is meant to enable" | d, e |
| C011 | "certifies 89% of in-spec tablets in real time at a verified 93% coverage" (`page.jsx:126`); "89% release at a verified 93% coverage ... best at a genuine guarantee" (:266-267, :319) | "Verified >=90% coverage at deployment" (`research.js:52`); HEADLINE yield 89 / coverage 93 (`research.js:14-20`); `figures.jsx:57`, `Charts.jsx:131`, `Benchmark.jsx:119` "verified 90% coverage"; `research.js:104` VIEWS blurb "at a verified 90% guarantee"; artifact working tree:296 ("at a verified 93% coverage"), 297, 299, 339 | `benchmark_summary.json methods.ours_best`: release_yield 0.8917, coverage 0.9277, mean_width_mg 10.53, false_release 0.0054 (100 seeds, measured); `guarantee_summary.json methods.cvplus_10fold_alpha0.1.proved_floor`=0.7150 (Barber et al. 2021 Thm 4, n=155, K=10); `methods.rrcm_full_conformal`: proved_floor 0.90, coverage 0.95, release_yield 0.8688, false_release 0.0028; `exchangeable_split_diagnostic.cvplus.coverage`=0.9075 and `.rrcm.coverage`=0.9048 on an exchangeable re-split (`benchmark_exchangeability.exchangeable`=false, KS p=3.4e-7); working-tree README:68-75 retracts the headline | numbers supported as measurements; "verified/genuine >=90% guarantee" contradicted by the theorem the site cites | "CV+ released 89.2% of in-spec tablets at a measured 92.8% coverage, but its proved finite-sample floor at n=155 is 71.5%. At a proved 90% floor (full-conformal ridge, RRCM) the release yield is 86.9% with 0.3% of released tablets out of specification. Measured coverage on the official split is inflated 2-4 points by a non-exchangeable calibration/test split." | d, f |
| C012 | Benchmark comparator rows (`research.js:29-33`; rendered by `BenchmarkTable`, `VIEWS[0]` :108-115): RTRT 85.4 / 94.2 / 13.0; MAPIE split 81.1 / 95.7 / 17.7; MAPIE CV+ 88.1 / 93.7 / 11.4; jackknife+ 90.3 / 89.8 / 10.2; crepes 55.1 / 93.5 / 60.1 (yield % / coverage % / width mg); "matches ours to 0.48 mg" (:31; `page.jsx:266`; `Benchmark.jsx:119`) | artifact working tree:323: jackknife+ "under-covering (89.9%, below the 90% it claims)" (site :32 says 89.8; stored 0.9080) and "within 0.5 mg" for the MAPIE match (site says 0.48) | `benchmark_summary.json methods.*` (yield / coverage / width): rtrt_fixed 0.8438 / 0.9668 / 13.45; mapie_split 0.8426 / 0.9421 / 15.26; mapie_cv 0.8922 / 0.9279 / 10.51; mapie_jab 0.9032 / 0.9080 / 9.78; crepes_norm 0.6601 / 0.9341 / 38.73; ours_best 0.8917 / 0.9277 / 10.53; `cvplus_vs_mapie_mean_width_gap_mg`=0.3446 (README says 0.50, site says 0.48). Working-tree README:124-127: an earlier table ran comparators at a 110-unit budget, "which inflated the lead to 5.8 points. Fixed." | contradicted: only the Telo row and the jackknife+ yield match any stored JSON; the comparator rows appear to be from the superseded 110-unit run (no stored JSON holds them) | replace the five comparator rows with the `benchmark_summary.json` values above (to one decimal: RTRT 84.4 / 96.7 / 13.5; split 84.3 / 94.2 / 15.3; CV+ 89.2 / 92.8 / 10.5; jackknife+ 90.3 / 90.8 / 9.8; crepes 66.0 / 93.4 / 38.7), add the RRCM row (86.9 / 95.0 / 12.2, proved 90%), add a "proved floor" column, and write "within 0.5 mg" for the MAPIE match | d |
| C013 | "Every hyperparameter is selected by cross-validation on calibration data alone" (`page.jsx:193-194`) | `page.jsx:240` "cross-validated field" | `research/conformal/experiment_tablet.py:23` `N_PLS = 8  # chosen by held-out R2 on instrument 1 (R2~0.90)` (whitespace collapsed) with no selection code; the parenthetical is the only stated criterion; corn `summary.json config.n_pls`=12 fixed; 03 audit section 4: a sweep puts the argmax at 4 components (R² 0.907 test, 0.956 hold-out) against 8 (0.878 / 0.935); feasibility CV mode selects 4.1 components on average and changes the drift headline (C003, C005). The release-yield benchmark does select by CV (`benchmark_summary.json config.lasso_alpha`=0.5796; `sota_summary.json config.pls_grid`, `prep_grid`); working-tree README:90 notes the RRCM ridge penalty is CV-selected then fixed | contradicted for the drift experiments; supported for the release-yield benchmark | "Point-model hyperparameters in the release-yield benchmark are selected by cross-validation on the calibration tablets. The drift experiments use a fixed component count (8 for tablets, 12 for corn) set before the runs; an independent re-run with CV-selected components (mean 4.1) and no standardization gave shifted coverage 72% instead of 41% and released 56% of shifted tablets instead of 1%, at 29% conditional error." (`ra_20260902T044654Z metrics.shift.empirical_coverage` 0.717, `released_fraction` 0.561, `conditional_error_among_released` 0.290; reproduction `ra_20260902T045127Z` 0.410 and 0.010.) | d, f |
| C014 | "Phlow Corporation got a BARDA contract worth up to $812M" [V] (`docs/startup-outline.md:113`, :160); "a contract worth up to $812 million" (EV proposal:37) | deck html:375 "$696.7M"; `deck-prompt.md:55` "$696.7M HHS contract"; outline:113 itself flags the inconsistency; HA-19 | `feasibility/data/raw_snapshots/S27/2026-09-01/usaspending_phlow.json results[1]["Award Amount"]`=696665953.98, recipient PHLOW CORP., start 2020-05-18, end 2027-01-13 (contract 75A50120C00092); press release: up to $812 M = $354 M base + $458 M options (05_inconsistencies I-C6) | both supported with different meanings | "Phlow's 2020 HHS/BARDA contract shows $696.7 M on USASpending (75A50120C00092, checked 2026-09-01); the announced ceiling including options was $812 M" | b, f |
| C015 | "The operating system for sterile drug manufacturing" (`layout.jsx:5, 9`; `content.js:4-5`); "The moat is the operating system, not the algorithm" (deck html:315); "protecting the operating system that instantiates it" (`page.jsx:331-332`) | none beyond the four at left; the company's own deck brief says the opposite (`funding/deck-prompt.md:61`) | `os/quality-intelligence/README.md:3` calls the component "one of four" of "the Telo plant operating system" and :335 "Advisory. Not a batch release decision."; `docs/roadmap.md:29` "No wet lab, no furosemide, no sterile injectable, no Telo-generated spectra". 05_inconsistencies I-C13 confirmed: `funding/deck-prompt.md:61` describes the software as a "manufacturing-intelligence layer" and ends with the instruction (Do NOT call it a "manufacturing OS."); the website headline (`layout.jsx:5, 9`; `content.js:4-5`) and deck html:315 contradict the company's own deck brief | contradicted by the company's own deck brief (`deck-prompt.md:61`); unsupported as a description of an existing product | keep only with a stage qualifier ("software in development for ...") or use "release-decision support layer"; do not pair "operating system" with "automates the release decision" | d, f |
| C016 | "Telo Research · Preprint" (`page.jsx:108`) | none | `paper/SUBMISSION.md` items 1-6 (not submitted; code archive and DOI do not exist); root `CLAUDE.md:74-75` "COMPLETE and NOT SUBMITTED" | unsupported (no preprint is posted) | "Telo Research · Technical note (manuscript in preparation)" | d, f |
| C017 | "within 2.4 points of the information-theoretic ceiling" (`page.jsx:131, 277-286, 320`; `research.js:89-95, 133, 137-138`) | Figure 3 caption :286; closing box :320 | `frontier_summary.json constant_width_oracle.symmetric.spec_yield`=0.9158 minus `cvplus_deployed.spec_yield` 0.8917 = 0.0241; but `gap_to_constant_width_floor.yield_points`=0.0489 against the min-width oracle (0.9406); `config.note` "oracle rows peek at test labels -> lower bounds only"; `max_possible_adaptive_gain_mg`=1.595; working-tree README:482-486: "An earlier version of this file claimed its oracle bounded every method ... That was wrong: the oracle ... bounds constant-width methods only" | contradicted as "information-theoretic"; the 2.4 is the more favourable of two constant-width oracles | "2.4 yield points below the symmetric constant-width oracle for this point model (4.9 below the minimum-width oracle); the oracle bounds constant-width methods only, and the measured adaptivity headroom is at most 1.6 mg of width" | d, e ("information-theoretic"), f |
| C018 | "The strongest models agree with one another to 0.68 mg while missing the labels by 4.1 mg ... what is left is the reference assay's own measurement noise. ... The ~4 mg floor is demonstrated, not assumed." (`page.jsx:248-251`; `research.js:84-85`) | artifact working tree:339 and EV proposal:31 already retract it | `pointmodel_summary.json residual_is_reference_noise_claim.verdict` "argument invalid; conclusion half right and quantitatively wrong"; `reference_method_floor_mg`=1.3; `residual_is_reference_noise_claim.headroom_mg`=2.43; `error_floor.top_model_disagreement_mg`=0.372 | contradicted by the repo's own later run | "The benchmark's reference method is stated at +/-1.3 mg; the deployed model's 4.1 mg RMSE leaves about 2.4 mg of non-reference headroom that a wide model search did not close." | d, e |
| C019 | "test RMSE 4.8 -> 4.1 mg, R² 0.907 -> 0.931" (`page.jsx:246`; `research.js:81-82`) | none | 4.1 mg and 0.931: `pointmodel_summary.json previous_version_best.test_rmse`=4.129, `bestmodel_summary.json lasso_test_r2`=0.9305; the PLS baseline 4.8 mg / 0.907 was not found under any key in `pointmodel_summary.json`, `bestmodel_summary.json` or `transfer_summary.json` (untraced; `transfer_summary.json tablet.rows[38]` has rmsep 4.80 / r2 0.9069 but is a Lasso PDS transfer row, `config.point_model`="Lasso on sg1_snv", not the PLS baseline); `sota_summary.json point_model.sota_r2`=0.904 is the nearest stored PLS figure; the site's "from ~81%" (`page.jsx:247`, `research.js:91`) does trace, to `bestmodel_summary.json pls_cvplus.yield`=0.8154 | partially traced | keep "4.1 mg, R² 0.931 (sparse-linear)"; state the PLS baseline only after Phase G re-derives it | d, f |
| C020 | "restores full coverage from nine reference assays" (`page.jsx:129, 228-230`; Figure 2 :232; `research.js:68-75`) | artifact working tree:282 | `operating_summary.json recal_cost.disjoint[1]`: n=9 coverage 0.9009, width 48.3 mg (paired 0.9057); n=5 no finite interval; site RECAL values 90.1 / 93.5 / 90.5 / 90.0 / 90.4 match `disjoint[1,3,4,6,8]`; width ratio `spec_yield.recal_width` 47.8 / `home_width` 22.9 = 2.1; `multisite_summary.json per_site_calibration_budget["9"].coverage_at_new_site`=0.855 with the Lasso model | supported with caveats | "with nine reference assays on the new instrument, marginal coverage returned to 90% at roughly twice the home interval width (48 vs 23 mg); in the multi-site configuration nine assays gave 85.5%" | d, f |
| C021 | "On the corn set ... 91% coverage becomes ~1%" (`page.jsx:207`) | README@HEAD:52, 61 | `summary.json properties.protein.instruments.m5.naive_coverage.mean`=0.9113; `mp5`=0.0143; `mp6`=0.0113 (300 seeds) | supported | unchanged; name the property ("protein") and the instruments (mp5, mp6) | f |
| C022 | "0.5% of releases out of specification" (`page.jsx:126`; `research.js:17`) | none | `benchmark_summary.json methods.ours_best.false_release`=0.0054 (measured); `risk_summary.json rcps_certified."risk<=0.05".certified_risk_bound`=0.0295 at delta 0.05; `"risk<=0.02".certified`=false (reason: "no gamma in [1.0, 3.0] certifiable at delta=0.05 with 100 calibration tablets"); `reference_tablets_needed_to_certify."risk<=0.02".if_zero_losses`=150, `."risk<=0.01".if_zero_losses`=300 | supported as a measurement; not a certified rate | "0.5% of released tablets fell outside specification (measured); the certifiable bound with the 100 calibration tablets available is 3% at 95% confidence; a 2% bound is not certifiable at that size (150 tablets would be needed with zero observed losses, 300 for 1%)" | d, f |
| C023 | "Telo builds and operates automated microplants" (`page.jsx:350`); Engagements tiers "Built against FDA PAT guidance", "A registered line of your own", "One validated recipe per drug" (`Engagements.jsx:21, 26, 34`) | root README:25 "Pre-data, pre-draft" | `docs/roadmap.md:29`: no wet lab, no sterile injectable, no Telo spectra; all 15 regulatory gates UNCERTAIN (`feasibility/config/regulatory_gates.yaml`); protocol forbids "FDA-ready" (:495) | unsupported as present tense | "Telo is building ..."; tier features as "planned" or "scope"; "Designed to fit FDA's PAT framework; no product-specific validation exists yet" | d, f |
| C024 | "the theorem behind CV+ only proves 1-2alpha, i.e. 80%" (artifact working tree:323); "the widely used method here proves only 80 percent while measuring 93" (EV proposal:31) | artifact:318 in the same file already says 71.5% | `guarantee_summary.json methods.cvplus_10fold_alpha0.1.proved_floor`=0.7150 (`kfold_penalty` 0.0850); working-tree README:70-72 "An earlier version of this README quoted 80% here ... it was wrong" | contradicted (stale) | "10-fold CV+ at alpha 0.10 proves 71.5% at n=155 while measuring 92.8%" | b, f |

## 2. Arithmetic, and the wording that holds under both pipelines

Two pipelines produced the tablet drift numbers. They agree only when the second one copies the first one's fixed choices.

| Quantity | Repository run (`experiment_tablet.py`, 200 seeds, 8 PLS components, sklearn PLS default scale=True, 105/50 split of the 155 calibration tablets) | Feasibility reproduction mode (`ra_20260902T045127Z`, 50 seeds, same choices) | Feasibility, 8 components, no standardization (`ra_20260902T044739Z`) | Feasibility, CV-selected components, no standardization (`ra_20260902T044654Z`) |
|---|---|---|---|---|
| shifted coverage | 0.4159 | 0.410 [0.328, 0.493] | 0.768 [0.733, 0.799] | 0.717 [0.685, 0.746] |
| released fraction on shift | 0.01097 | 0.010 [0.001, 0.027] | 0.303 [0.260, 0.347] | 0.561 [0.523, 0.602] |
| unconditional wrong release (interval miss among all shifted tablets) | 0.00739 | 0.0058 [0.0007, 0.0137] | 0.046 [0.036, 0.055] | 0.155 [0.137, 0.172] |
| conditional error among released | 0.674 (= 1 - 0.3260, 23 seeds) | 0.609 [0.272, 0.881] (7 seeds) | 0.160 [0.137, 0.185] | 0.290 [0.256, 0.327] |

Keys: `research/conformal/results/tablet_summary.json instruments.shift.*`; `feasibility/results/release_assurance/<run>/metrics.json metrics.shift.{empirical_coverage, released_fraction, unconditional_wrong_release, conditional_error_among_released}.{mean, ci95_low, ci95_high}`; table also in `feasibility/docs/methods/release_benchmark.md:7-13`.

Derived numbers: 58.4% = 1 - 0.4159 (C004). 0.74% = `misrelease_gated` = 0.00739 (C005). 3% on the website has no tablet source; the corn value 0.0348 x (1 - 0.0037) = 0.0347 (C007). 89.2% and 92.8% are `benchmark_summary.json methods.ours_best.release_yield` and `.coverage`; 71.5% is `guarantee_summary.json methods.cvplus_10fold_alpha0.1.proved_floor` (C011). 2.4 points = 0.9158 - 0.8917 (C017). $696.7 M = 696,665,953.98 / 1e6 (C014).

Wording true under both pipelines:

- W1 (drift, gated): "Under the repository's pipeline (8 PLS components with per-wavelength standardization) the gate cut the interval-miss exposure on the second instrument from 58% to about 0.7% by abstaining on 99% of tablets; under a standard pipeline (cross-validated components, no standardization) coverage fell to 72% and the gate released 56% of shifted tablets with 29% conditional error. In neither case does the gate release at useful quality under shift; what it supports is stop-the-line detection."
- W2 (drift, coverage): "Nominal-90% coverage fell to 42-77% on the second instrument depending on preprocessing; the collapse is real in every mode, its size is not a fixed property of the data."
- W3 (release yield): "89.2% measured release yield at 92.8% measured coverage (CV+, proved floor 71.5%); 86.9% at a proved 90% floor (RRCM); 0.3-0.5% of released tablets out of specification, measured, with a certifiable bound of 3%."

## 3. Remove and qualify lists

Remove (class e):
- "automates the release decision" (`layout.jsx:7`, `content.js:6`). Contradicted by the OS component's own advisory status and by the protocol claim-discipline table.
- "near 3%" and `shiftGated: 3` (`page.jsx:128-129, 209`; `research.js:63`). No tablet source; corn number in a tablet figure.
- "information-theoretic ceiling" (`page.jsx:131, 286, 320`, and the surrounding argument `:277-284`; `research.js:133, 138`). The oracle bounds constant-width methods only.
- "what is left is the reference assay's own measurement noise ... ~4 mg floor is demonstrated" (`page.jsx:248-251`). Retracted by `pointmodel_summary.json` and by the artifact's own working-tree text.
- Benchmark comparator rows as stored (`research.js:29-33`). Replace with `benchmark_summary.json` values.
- "Verified >=90% coverage at deployment: Telo yes" (`research.js:52`), "verified 90% coverage" captions (`figures.jsx:57`, `Charts.jsx:131`, `Benchmark.jsx:119`) and the VIEWS blurb "at a verified 90% guarantee" (`research.js:104`). Replace "verified" with "measured" and add the proved floor.
- Root README beachhead line as written (`README.md:7`).

Qualify (class f): C001 (615 not 654), C002 (CI), C003 and C005 (pipeline caveat, W1/W2), C004 (interval-miss vocabulary), C009 (injectables vs oral solid dose), C011 (measured vs proved, W3), C013 (which hyperparameters were CV-selected), C014 (obligated vs ceiling), C015 (stage qualifier), C016 (not a preprint), C019 (PLS baseline untraced), C020 (width doubles; multi-site 85.5%), C021 (name property and instruments), C022 (measured vs certifiable), C023 (present tense), C024 (71.5% not 80%), deck proof bars (label as corn).

## 4. Ordered change plan

(i) Working-tree edits the agent may make now (inside `feasibility/` only; listed, not all made by this pass):
1. `CLAIMS_REGISTER.csv`: rows C001-C010 updated and C011-C024 added (done in this pass).
2. `docs/audits/07_human_action_queue.md` HA-04: widen from "654 / near 3% / benchmark rows / beachhead" to the full C001-C024 list and point here (not done; one-line edit).
3. `docs/methods/release_benchmark.md:17`: "releases ten times as many shifted tablets" understates its own no-standardization ratio (released 0.303 against 0.010 in reproduction mode, 30x; the CV mode is 56x); write the fractions or the ratios (not done).
4. `RESEARCH_LOG.md` / `CHANGELOG.md`: record this document (not done).

(ii) Founder approvals needed (class b), in the order that removes the most exposure first:
1. HA-04 extended: approve the replacement texts in section 1 for `telo-web` (C001-C004, C007, C009-C013, C015-C023).
2. Root `README.md:3` and `:7` (C009, C008) and root `CLAUDE.md:3` (C009): these are public at HEAD.
3. Commit and push the working-tree `research/conformal/README.md` and artifact so the public copies carry the retractions (C005, C011, C018, C024); this needs HA-05 (commit authorization) and a decision on whether the artifact's working-tree version is the intended public one; that version still says "at a verified 93% coverage" (:296) and gives jackknife+ coverage as 89.9% (:323; stored `benchmark_summary.json methods.mapie_jab.coverage`=0.9080, so 90.8%), and both need editing before it becomes the public copy.
4. `docs/startup-outline.md:84-85, 113, 160` (C008, C014) and `funding/emergent-ventures-proposal.md:31, 37` (C024, C014) if that proposal is still to be sent or has been sent.
5. Decide which Phlow figure the company quotes (HA-19) and use it everywhere.
6. Decide whether "operating system" stays as positioning with a stage qualifier (C015); the company's own deck brief (`funding/deck-prompt.md:61`) already says not to call it a manufacturing OS.

(iii) Website deploy steps (class d), after (ii)1:
1. Edit `telo-web/src/data/research.js` (HEADLINE, BENCHMARK, CAPABILITIES row 6, DRIFT, FRONTIER, VIEWS, header comment lines 1-12 which currently claim every number comes from `results/*.json`), `src/app/research/page.jsx` (abstract, sections 3, 4, 7, 8, 9, closing box, "Preprint" eyebrow), `src/app/layout.jsx:5-9`, `src/data/content.js:4-6`, `src/components/sections/Engagements.jsx:18-34`, and the four "verified" captions and blurbs (`figures.jsx:57`, `Charts.jsx:131`, `Benchmark.jsx:119`, `research.js:104`).
2. Add a `source` field per number in `research.js` naming the JSON key, so the header comment becomes true.
3. `npm run build` in `telo-web/`; review the rendered /research page against section 1.
4. Commit (HA-05) and push; Vercel project `telo-web` builds from the repository (confirm the linked branch in the Vercel dashboard; not checked here). Verify the live page, then record the deploy date in `CLAIMS_REGISTER.csv last_checked`.

(iv) Public-history remediation (class c): not needed for the claims. Wrong numbers in past commits are ordinary corrections; keep the history and let the corrected commit and this document be the record. The separate privacy issue is different in kind: `outreach/lab-contacts.md` is tracked and public (102 lines; 30 lines match an e-mail pattern; not reproduced here) while `paper/SUBMISSION.md` says the repository "must not be published". That one does need a founder decision on history rewriting or repository visibility (HANDOFF section 6); it is outside this document's scope.

## 5. Gaps only Phase G can close

| Number | Why the stored artifact cannot settle it | Script that would produce it |
|---|---|---|
| Confidence intervals on 0.74% gated exposure and on 32.6% conditional coverage at 200 seeds | `tablet_summary.json` stores means only; the 03 audit's bootstrap intervals (for example [0.17, 0.50] on the 32.6% conditional coverage; 5,000 resamples over seeds, 03 audit section 3) exist in audit prose only, not in any JSON | `feasibility/scripts/run_release_benchmark.py` in reproduction mode with 200 seeds (writes `metrics.json` with `ci95_*`), or `research/conformal/experiment_tablet.py` extended to write per-seed arrays |
| The component-selection rule behind `N_PLS = 8` | no selection code exists; the audit sweep (argmax 4) lives only in audit prose | a selection sweep added to `run_release_benchmark.py` (the CV mode exists: `ra_20260902T044654Z`, mean 4.1 components) with the sweep table written to `metrics.json` |
| Website comparator rows (C012) | no stored JSON matches them; the 110-unit-budget run was overwritten | re-run `research/conformal/benchmark_release.py` and pin the output hash in a manifest; the website should then read only that file |
| MAPIE width gap: 0.34 (JSON) vs 0.50 (README) vs 0.48 (site) | three values, one key | `benchmark_release.py` and `validate_core.py` re-run; report the JSON key only |
| PLS baseline 4.8 mg / R² 0.907 (C019) | no key found in `pointmodel_summary.json`, `bestmodel_summary.json` or `transfer_summary.json` (the latter's `tablet.rows[38]` 4.80 / 0.9069 is a Lasso PDS transfer row, not PLS) | `research/conformal/point_model_bakeoff.py` (writes the per-model table) |
| Corn gated-exposure formula (C007) | inferred here as release_rate x (1 - coverage_on_released); the script's printed table is not stored | `research/conformal/experiment.py` with the derived exposure written to `summary.json` |
| Recalibration at n=9: 90.1% (PLS, `operating_summary.json`) vs 85.5% (Lasso, `multisite_summary.json`) | two configurations, no reconciliation | `experiment_operating.py` and `experiment_multisite.py` on one shared configuration |
| Symmetric vs minimum-width oracle gap (2.4 vs 4.9 points, C017) | both stored; which one the site should cite is a wording choice, but the adaptive-oracle bound has no CI | `research/conformal/frontier_check.py` with seeds written out |
| Public artifact HTML content | only occurrence counts of the HEAD version were checked | none (read `git show HEAD:research/conformal/artifact/release-layer.html` before deciding what to publish) |

## 6. Numbers not traced in this pass

- Website PLS baseline "4.8 mg, R² 0.907" (C019): untraced to a JSON key (`pointmodel_summary.json`, `bestmodel_summary.json`, `transfer_summary.json` checked); the adjacent "from ~81%" does trace (`bestmodel_summary.json pls_cvplus.yield`=0.8154).
- Jackknife+ coverage: artifact working tree:323 says 89.9%, `research.js:32` says 89.8, `benchmark_summary.json methods.mapie_jab.coverage`=0.9080; neither public value traces to the stored key.
- Website `research.js:126` "Same sensor" Telo bar = 8: matches only the corn home gated value (7.9%), untraced for tablets (tablet home gated is 6.6%).
- "0.48 mg" MAPIE match (`research.js:31`, `page.jsx:266`): untraced; stored key gives 0.34 mg.
- Live website URL and the Vercel production branch: not verified.

## 7. Added 2026-09-03: a contradicted citation in the repository's own release memo

The prior-art sweep's family-11 note flagged, and this session verified against the frozen snapshot, that `regulatory/release-constraints.md` lines 96 to 98 attribute to FDA's 2004 aseptic processing guidance a sentence that is not in it. The paragraph is marked `[V]`.

| item | value |
|---|---|
| Claim as written | FDA 2004 aseptic guidance section XI.B: indirect measurements in aseptic processing give limited information about whether a batch is sterile, so "the sterility test is an essential element." |
| What the guidance contains | The string "essential element" occurs once in the document, at section XII: "Review of environmental and personnel monitoring data, as well as other data relating to acceptability of output from support systems ... are considered essential elements of the batch release decision." Section XI.B reads: "Sterility tests are limited in their ability to detect contamination because of the small sample size typically used." |
| Source | `data/raw_snapshots/S07/2026-09-01/fda_aseptic_processing_2004.pdf`, text extracted 2026-09-03 |
| Verdict | contradicted by the primary source |
| Effect on the memo's conclusion | none. The 14-day pole rests on USP <71> incubation and on sterility samples being drawn during the fill, not on this sentence. The environmental-monitoring floor at line 70 quotes the section XII sentence correctly. |
| Change class | (b) founder approval, then (a) working-tree edit; the file is outside `feasibility/` so this session did not edit it |
| Registered as | CLAIMS_REGISTER.csv C025; queue item HA-10 |

The reason this matters more than a footnote: the sentence is doing argumentative work in the paragraph that establishes why the release layer cannot shorten the hold, and it is one of the few places in that memo carrying a `[V]` tag. Section XI.B in fact argues that the sterility test detects very little, which supports process control over end-product testing. That is a different argument, and a stronger one, but it is not the one the memo makes.
