# Research log

Chronological record of questions, decisions, experiments, failed approaches, and revisions. Newest entries at the bottom. Dates are absolute. Entries written with agent assistance are marked `[agent-assisted]`; see `AI_ASSISTANCE.md`.

## 2026-09-01

**Inputs located.** `Telo_Feasibility_Study_Protocol.docx` (sha256 9c0e0a0a...e15dd), `.pdf` (36bf2c08...7f07ea), `Telo_Feasibility_Model.xlsx` (36706951...9be9f8) found in `~/Downloads`. Both protocol renderings carry "Version 1.0 | 1 September 2026". Workbook cover says "0.1 research scaffold", as-of 2026-09-01, model status "ILLUSTRATIVE - DO NOT CITE OUTPUTS", 13_Checks reports "NOT READY - RESOLVE CHECKS" (3 of 9 checks OK). `[agent-assisted]`

**Repository state.** Real Telo repo is `~/telo` (remote `git@github.com:rikhinkavuru/Telo.git`, branch main, last commit c3b31b9 2026-07-26). Working tree dirty: 12 modified files (CLAUDE.md, docs, research/conformal README and core, graphify) and many untracked (paper/, os/quality-intelligence/, regulatory/*.md, conformal experiments and results). None touched. The session's launch directory `~/feasibilitymodel_telo` was empty and sits inside a home-directory git root with no remote; not used. `[agent-assisted]`

**Decision D001.** Build the feasibility package at `~/telo/feasibility/` as a self-contained subproject (own `pyproject.toml`, `uv.lock`), following the precedent of `os/quality-intelligence/`. Rationale in `DECISIONS.md`.

**Protocol frozen as data.** `protocol/protocol.yaml` v1.0.0 written from the full DOCX text (all 13 sections, appendices A-F). Two numeric choices the protocol leaves open were pre-specified before any result exists: tail confidence q = 0.90 and recovery window 30 days (revisions R001). Gates: 15-row superset from workbook 04_Reg_Gates, all UNCERTAIN (R002). `[agent-assisted]`

**Workbook read (own reading, not only agents).** Deterministic sheet 09 is a 22-column row per product-strategy driven by 05_Assumptions, 06/07 product sheets, and 08_Strategies. Observed defects to reconcile in code: (a) capital annualized with CRF but validation annualized straight-line over economic life; (b) expiry/scrap counted in variable cost gross-up (1/(1-scrap)) and again in inventory-and-expiry cost; (c) testing batches = demand/units-per-batch, ignoring yield; (d) cost per unit divides by demand, not delivered units (Eq. 4); (e) CRF is a typed yellow value rather than a formula; (f) utilization exceeds 100% for status-quo and safety-stock rows at base inputs (13_Checks row 10 fails, 14 of 16). `[own reading]`

**Conformal reproduction (audit agent, verified against files).** `experiment_tablet.py` reproduces `results/tablet_summary.json` byte-for-byte in 2.75 s. Claims map: R2 0.877 -> 0.046; naive coverage 0.914 -> 0.416; naive mis-release 0.584; gated mis-release 0.0074 (the "~1%" is the script's console rounding); release rate 0.011 (abstain 98.9%); coverage-on-released 0.326, which is a mean over the 23 of 200 seeds that released anything (pooled 308/1009 = 0.305). No confidence intervals exist in the script. N_PLS=8 is hard-coded with a comment about held-out selection whose code is absent; a sweep finds 4 is the argmax under both criteria. Data file holds 655 tablets, the experiment uses 615, public text says 654. The marketing site says the gate holds mis-release "near 3%", which matches no stored tablet number. `[agent-assisted, reproduced]`

**Audits completed.** Ten audit agents returned (workbook sheets A-D, conformal reproduction and code review, repository claims, evidence inventory, OS package, public-source probe). The adversarial verification pass (60 skeptic agents) and the requirements-matrix agent were killed by a session limit before returning; the matrix was written by the main agent instead and the anomaly registers carry one audit reading plus the main agent's own direct reading. `[agent-assisted]`

**Package built (Phase 1).** `schemas.py` (15 entity types, unit/range/date/provenance validation), `provenance.py`, `configs.py`, `economics.py`, `workbook.py`, `deterministic.py` (workbook replica + corrected variant), `regulatory.py` (gate eligibility, R3 lock, 503B state), `reporting.py` (22 definition-of-finished tests), `cli.py`. Workbook reconciliation: 320/320 screen cells and 128/128 dashboard cells match. 53 tests green at end of day; ruff and mypy --strict clean.

**Source acquisition run.** 37 payloads frozen from 24 auto sources (openFDA, Drugs@FDA, Orange Book, DailyMed, CMS Part B and ASP, FDA guidances, GAO, DSTF, eCFR versions + GovInfo CFR XML, Federal Register 2026-14073, USASpending). Human tasks recorded for ASHP (Cloudflare challenge, copyrighted), two paywalled papers, and the two accessdata sources outside their robots visiting window. `[agent-assisted]`

## 2026-09-02

**Accessdata sources fetched inside the 23:00-05:00 EDT window** (00:01 EDT): FDA shortage CSV export (539,616 B, 1,635 rows), two detail pages, NDC directory (10.8 MB). 41 payloads total.

**Product screen run** (`scripts/build_product_dossiers.py`): six dossiers. Two parser defects found and fixed on real data: the FDA export starts with a blank line and pads column names with spaces (parsed to zero rows until fixed); Drugs@FDA regex matching pulled in combination and oral products until exact single-ingredient matching on the ActiveIngredient field was used. Results: sodium bicarbonate 19 rows current since 2017-03-01, 13 injectable holders, not on the 503B bulks list; furosemide 33 rows current since 2020-04-07, 28 holders; sterile water 21 rows since 2021-11-23, 1 holder; norepinephrine, acyclovir, acetazolamide 0 rows. Acetazolamide fails PG2 (lyophilized). No candidate selected; 0.55 of the score weight is expert-pending. Report: `docs/methods/product_screen_2026-09-02.md`.

**Regulatory maps drafted** (`docs/regulatory/`), labeled not legal advice; all 15 gates remain UNCERTAIN.

**Status:** `make status` reports definition-of-finished progress; DF11 (reconciliation) passes; DF02 (dated five-product screen) passes once the screen manifest carries snapshot ids; everything else open. Milestone 1 items 1-12 of the task specification are complete except the human-dependent parts.

**Phase 3 engine built (2026-09-02).** Modules: `rng` (entity-keyed SeedSequence streams), `disruptions` (exogenous world for a universal roster), `demand`, `inventory` (FEFO, integer units), `allocation` (four policies incl. LP), `suppliers`, `production`, `quality`, `release_assurance` (R0-R3; R3 refused unless permitted; assay is the only reducible component), `strategies` (S0-S7 topologies), `simulation` (nine-step daily loop, mass-balance assertion), `runner` (multiprocess paired runs, MCSE, paired differences). Engine speed 0.04-0.08 s per strategy-run; 100 runs x 8 strategies x 2 products in 25 s on 8 cores.

**Two defects found by the tests, both real.** (1) The property test found fill rate > 1: served units included warm-up backlog; fill rate is now demand fulfilled within the backorder window attributed to origin day (protocol B1). (2) The daily mass-balance assertion failed at day 1730 of the first full-horizon run: a shipment that expired in transit was counted as served; expired-in-transit lots now go straight to waste and served is the quantity actually issued.

**Illustrative first pass (not results).** With workbook inputs the status quo for sodium bicarbonate is capacity-infeasible (fill 0.71), so every added-capacity strategy "improves" trivially; this is why protocol FC2 requires optimization to a matched service target before comparison. Regional nodes with the central plant's batch size waited on materials (a node batch was 49 days of node consumption); node batch size now scales with node scale (WB-04 in reverse).

**Optimizer** (grid + coordinate refinement + full-N re-evaluation, per strategy, multiprocess) running in background at search 20 / final 100 runs. **Sensitivity** (one-way, Sobol, PRCC, decision-reversal map, EVPI/EVPPI), **backcasting** (3 dated episodes from the FDA snapshot + 1 template), **figures with mandatory metadata**, **report builder**, **interview and reviewer infrastructure** written. Backcasts of the three ongoing shortages are consistent only because a chronic capacity gap never recovers; that is a weak check and is labeled as such.

**Tool note.** The editing MCP tool hit its plan cap mid-session; files from then on were written through shell heredocs and Python patch scripts. Two patch scripts silently missed their anchor once each (optimizer worker function), caught by mypy and the run log.

**Release-assurance benchmark (2026-09-02).** Independent re-implementation on the frozen IDRC tablet data (S28). Reproduction mode (8 components, sklearn per-wavelength standardization) recovers the repository's picture: shifted coverage 0.45, released 0.04. Turning off the standardization halves the collapse (0.79) and the gate releases 40%; CV-selected components (mean 4.1) give coverage 0.72 and 56% released with 29% conditional error. The headline stop-the-line result is implementation-sensitive; in no mode is the gate operationally useful under shift. Repair with 9 verified assays restores coverage near 0.90. Stress tests: SNV hides offset/gain/drift; wavelength shift, dead channels, noise, spikes collapse coverage with partial abstention. Recorded as candidate non-obvious finding NF-1; CLAIMS_REGISTER C005/C006 updated. `[agent-assisted; reproducible from results/release_assurance]`

**Optimization run** `opt_20260902T043854Z` (717 s; search 20, final 100 runs). See results summary in the report; all strategies NO_CONCLUSION by gate status.

**Optimization outcome at the frozen target (illustrative inputs).** With tau = 0.99 and q = 0.90 no strategy is feasible for either product: S0/S1 are capacity-bound for sodium bicarbonate (closest fill 0.73) and just below target for norepinephrine (0.988); S2/S3/S4 reach mean fill 0.993 but only 84-89% of runs clear 0.99 (the tail constraint binds on rare long disruptions); S5/S6 top out at 0.936/0.992 because nodes commission at day 730 of a 5-year horizon and share suppliers; S7 adds little. Safety-stock designs sit at their search bound (120 d), so the bounds and the tail constraint, not the mean, decide feasibility. Interpretation: under the workbook's illustrative disruption rates the frozen service target is unattainable within the searched design spaces; whether that survives real inputs is exactly what the evidence queue must answer. A tau = 0.98 optimization (pre-registered sensitivity value) is queued.

**Sensitivity run (sodium bicarbonate, optimized designs, 6 runs per scenario, Sobol N=16, VOI 40 samples; 398 s).** One-way sweeps flip the preferred strategy for 11 of 19 inputs, which says the ranking is fragile under illustrative inputs, not which strategy wins. Sobol total-order on the paired S5-minus-S4 annual cost: node_scale 0.86, fixed QA labor 0.11, everything else ~0 (the paired cost difference is fixed-cost dominated). EVPI on annual cost 4.8 M USD/yr with S3 the expected-cost best among feasible-when-feasible strategies; both numbers are artifacts of tier-5 ranges and exist to show the machinery, not to inform anyone. Report built with four metadata-checked figures; test report regenerated (100 passed); definition of finished 10/22.

**Threshold sensitivity (tau = 0.98, q = 0.90; run opt_20260902T050443Z_tau0.98, 570 s).** Dual source with contracts (S2) becomes the only feasible strategy for both products (norepinephrine 18.4 M USD/yr at capacity factor 0.6; sodium bicarbonate 26.1 M USD/yr at 1.07, both at the 120-day safety-stock bound and a shared API supplier). Every other strategy stays infeasible at full N, mostly on the tail constraint (P(fill >= 0.98) 0.71-0.89). Read as: under illustrative disruption rates the tail constraint, not mean fill, is binding, and only a second qualified finished-dose source clears it within the searched bounds. Not a finding; a demonstration that the frozen target, the tail rule, and the search bounds decide feasibility together.

**Session 1 close (2026-09-02).** Handoff written: `HANDOFF.md` (state, pointers, real vs illustrative, rules, tooling notes, weak spots, open decisions), `CLAUDE.md` (working rules for this directory), `docs/design_space/ASSIGNMENT.md` (next assignment verbatim: adversarial design-space expansion; Phase A failure decomposition first; new strategies as S8+). Persistent memory updated. Nothing committed.

## 2026-09-02 (session 2)

**Phase G, release-assurance follow-up (design-space assignment).** Audit of 654 / 655 / 615: the distributed `.mat` (S28, sha256 129a32ec...) holds 155 + 460 + 40 = 655 tablets on two instruments, the experiments use 615, and "654" is Eigenvector's page count quoted in `data_tablet.py:3`; every public number in C001-C007 and the README headline claims is mapped to its artifact key in `docs/design_space/release_assurance_followup.md` section 1.2, with proposed register updates (not applied; the claims-corrections track owns the file). Leakage audit of `research/conformal` (read only): no leak in the tablet drift numbers; the Lasso penalty for the comparator rows and the ridge penalty for RRCM were selected on the full reference set (the ridge case is acknowledged in the README and costs 0.7 coverage points, `guarantee_summary.json:methods.rrcm_in_bag_penalty`); the gate statistic in `experiment_operating.py` was chosen on the evaluation instrument; the stated `N_PLS = 8` selection rule is not reproducible. `release_benchmark.py` extended additively (DS and per-wavelength instrument standardization on transfer standards, AURC / E-AURC with tie handling, held-out shift-type matrix, USD cost curves from provenance-carrying parameters with a new tier-5 `wrong_release_cost_usd`, repair with n = 3, R2, gate and selection options); legacy keys bit-identical (265 values per mode, zero differences) because extension draws use separate streams and the bootstrap orders legacy keys first. Five 50-seed runs: `ra_20260902T222443Z` (reproduction), `ra_20260902T222725Z` (8 components, no standardization), `ra_20260902T222900Z` (CV components), `ra_20260902T223019Z` (selection leak: at most 0.5 points, negligible), `ra_20260902T223132Z` (Q-residual gate: releases 0.000 of shifted tablets against 0.561 for 1-NN at 2% false holds; the threshold tuned on the instrument change transfers to every harmful synthetic fault, the reverse does not). Selective prediction: excess AURC 0.15-0.26 on shift, so the gate detects change but does not rank which released units are right. Repair below nine verified assays gives no finite guarantee (n = 3 and 5: half-width +inf); nine restore coverage near 0.90 at 1.9-2.6x home half-width; PDS with 50 standards is the best label-free repair (0.90-0.93). Illustrative USD cost curve: at alpha = 0.10 the gate loses to testing every batch on the home instrument (125-151 k USD per batch against 30 k) unless a wrong release costs under about 0.4 M USD. Conclusion for the product question: release prediction is not defensible on this evidence; stop-the-line drift detection with a conformal limit on the industry's Q residual is, narrowly; conditions and evidence for the reverse are tabulated in the follow-up document. Tests 9 new (`tests/unit/test_release_benchmark_ext.py`); matrix rows R103-R108. `[agent-assisted; reproducible from results/release_assurance and scripts/run_release_benchmark.py]`

**Phase A, failure decomposition by ablation (design-space assignment).** Harness `ablation.py`: 22 named mechanisms switched off one at a time from the base world (leave-one-out) and on one at a time from the quiet world (add-one-in), 100 common-random-number runs each, both products, all eight strategies at the frozen-target optimizer's best-or-closest design (run `abl_20260902_phaseA`, 5411 s under heavy CPU contention). Headline model behavior (illustrative inputs, not findings): sodium bicarbonate S0/S1 sit at 124% status-quo utilization by construction and fail on capacity alone (fill 0.720 to 0.973 when nominal capacity is sized to 75%); every capacity-adequate strategy (S2-S4) reaches mean fill 0.993 and fails only the tail (P(fill >= 0.99) 0.79-0.89), which the base-stock review rule alone lifts to 0.93-0.98 and a year of base-stock inventory lifts to 1.00 at +2% cost; S5/S6 fail on the 730-day commissioning inside the five-year window (0.932 to 0.999 when removed); release queue and sterility incubation move fill by at most 0.003; S7 is feasible only when always legally eligible (0.866 to 0.997). Norepinephrine S0 reaches P(meet) 0.90 and S1 0.94 with the review rule alone, so the session-1 "no strategy feasible" conclusion was a model artifact for that product. Three model defects surfaced: MD-1 free raw-material stock with a lead-time-scaled buffer (fixed, R003); the (s, S) regional review rule with s = lane time + 1 day (kept as the frozen rule, exposed as an opt-in design variable); horizon-mean fill under a 730-day commissioning makes any new-build strategy fail the tail regardless of horizon. Supplementary runs: `abl_20260902_phaseA_mb` (material buffer; +0.001 to +0.015 fill, P(meet) +0.01 to +0.14), `abl_20260902_phaseA_pairs` (six-factor factorial). Decomposition document: `docs/design_space/bottleneck_decomposition.md`.

**Engine infrastructure for S8+ (same day).** Declarative site plans, warm standby with exercise batches and activation failure, hub-as-supplier, portfolio shares, unified strategy registry, unmapped-gate guard; S0-S7 proven byte-identical (`test_frozen_strategies.py`). Session rate limit at ~18:30 killed 42 of 50 evidence-sweep agents and the Phase G fork mid-run; all were resumed from cache at 22:30 and the benchmark runs were redone on final code.

**Protect-the-project track.** `docs/design_space/privacy_remediation_plan.md` (exposure is larger than the handoff said: 29 people with addresses in `outreach/lab-contacts.md`, up to 18 more in `outreach/facilities.md`; options A-D with founder decisions HA-06 to HA-09; nothing executed) and `docs/design_space/public_claims_corrections.md` (24 registered claims; two blockers found by the adversarial pass and fixed: the deck brief's own "do not call it a manufacturing OS" line exists, and the C013 wording had mis-attributed the mode-2 ratio).

**Model-defect fixes after the Phase A decomposition (2026-09-03, revisions R004 and R005).** The decomposition's section 7 listed sixteen defects; nine were fixed and seven deferred with reasons. The two that change conclusions are MD-7 and MD-14. MD-7: `capacity_factor` was rescaling an already-registered plant instantly while identical capacity added as a new site waited 730 days, which exempted the added-capacity strategies from the protocol's own fair-comparison rule. Capacity above the workbook's status-quo scale of 1.05 now waits `capacity_expansion_days` (tier 5, base 365 d, justified against the second-source qualification time already in the configuration). Sodium bicarbonate S4 falls from 0.9820 to 0.9750 mean fill and from 0.54 to 0.32 tail probability on the baseline designs, and 12 of the 32 frozen event digests change. MD-14: a reserved CDMO line was charged full capital, fixed operations, and validation as though Telo owned it, and then a reservation fee on top, which the protocol's cost boundary forbids; S3's annual cost falls about 40%. MD-6 (fixed operations before a site exists, and end-of-horizon backlog censored as lost at any age) removes 3.1 M USD/yr from norepinephrine S5 and adds the metric `open_backlog_units_at_horizon`. MD-4 (opening stock all stamped at half shelf life, which for a 24-month product is day 365, the first measured day) is fixed by seeding four spread cohorts; it had made larger safety stock read as harmful and it invalidated the optimizer's safety-stock conclusions, so every optimization result predating this revision has to be re-run before it is quoted. MD-2, MD-9, MD-10, MD-13 and MD-16 are corrections to the material reorder point, the utilization denominator, the Shapley bracket, the node cap, and the reporting of tail uncertainty. Deferred: MD-1 (decoupling the material buffer from lead time changes the ledger and needs its own revision and re-optimization), MD-3, MD-5, MD-8, MD-11, MD-12, MD-15, each recorded in the status table appended to section 7. Consequence for the session's conclusions: candidate NF-8, which compared inventory against distributed nodes on cost, rests on runs made before these fixes and stays suspended until the ablation, interaction, optimization and design-space runs are repeated.

**Verification pass over the Phase D configuration (2026-09-03, revision R007).** An adversarial review of `config/strategies/design_space.yaml` and the four Phase B/C/D documents returned findings at three severities; all blockers and majors were fixed and the minors taken. Four were substantive.

*Independence asserted where the configuration denied it.* Five designs (S9, S10, S11, S14, S19) omitted `cc_quality` on a separate-legal-entities or own-registration rationale while their own `regulatory_owner` field placed every site under one application, whose holder's quality unit performs a disposition that 21 CFR 211.22(a) makes non-delegable. S10's own field contradicted itself inside one sentence pair. All five now declare the group and state the reasoning, which is the form S12, S16 and S17 already used. S18 keeps no group and now says why (two partner ANDAs, two holders). Separately, every site of every design draws `stopper_1` and no design declared any container-closure group, so all twelve tails were optimistic by one shared-supplier hazard that four designs described only in prose; `cc_stopper_1` is now declared on every site plan. Because every declared group draws its own Poisson stream at one global rate, group count is not neutral, so the geography convention was also settled: `cc_geo_<region_id>` on every site, which removes the group-count gap between structurally identical designs (S8 had six groups and S14 four for the same two-site R1/R3 shape). S0-S7 carry neither the stopper group nor a wave-0 node geography group, so that asymmetry is recorded the way MD-3's review rule already is, and the matched-membership control was run for S18 first: shared 0.98711 (MCSE 0.00289) against split 0.98721 (MCSE 0.00288), a difference inside MCSE, so the container correlation is now represented but is not load-bearing at the frozen hazard rate.

*A supplier's common-cause group could not do anything (MD-19).* Group capacity is set to `1 - impact` with impact drawn from a Beta of mean 0.5, so it never reaches zero, while `MaterialStore.receive` gated shipment on capacity above zero. Any group declared on a supplier therefore shipped every order in full on every degraded day. Over 20 generated worlds `cc_ksm_api_1` had 12 events and 1,120 degraded days with a minimum capacity of 0.299 and no day at 0.0, so S9's and S19's shared key-starting-material tier, the whole point of both designs, could not reach the material channel at any parameter value. A supplier at fraction `f` now ships at most `f` of one full order per day, the same proportional time stretch `SiteRuntime.start_batch` applies to a degraded line. Full and zero capacity behave exactly as before, so no frozen event digest or fill moves. Fixing the structure did not make the mechanism visible: S9's matched-membership control still returns 0.997733 in both arms at n = 25, and that null is a property of network slack and the 0.1 events per group-year rate, not of the gate. Both register rows now say so.

*Contracted-capacity architectures were screened as the status quo (MD-20).* The deterministic screen dropped every reserved site from saleable capacity and charged no reservation fee, because `reserved_cost` reads `reserved_capacity_fraction`, which no S8+ design sets. S11, S15, S16, S17 and S19 screened at capacity 963,900, utilization 1.245 and chronic shortfall 77.7 days on sodium bicarbonate, byte-identical to S0, with no fee anywhere in cost per unit; `ablation.network_capacity_units_per_year` had already been corrected the other way under MD-9, so the package held two contradictory capacity definitions. A reserved line now contributes its exercise duty cycle (campaign activation is stochastic and excluded, so the credit is a lower bound) and its reservation fee plus take-or-pay. Sodium bicarbonate now separates them: utilization 1.105 for S11, 1.148 for S16, 0.993 for S17 and 1.194 for S19 against S0's 1.245. Four call sites (`cmd_deterministic`, two in `ablation`, one in `backcast`) were still loading only the frozen eight, so Phase D's screening and ablation batteries never saw S8-S19; they now use the full registry, while the workbook replica keeps the frozen eight and reconciles 448/448 unchanged. A related timing defect, MD-21, charged S16's reservation fee and take-or-pay for the 540 days before either contracted line could make commercial product; both now accrue only while the site exists.

*Two instruments that could not be measured.* `take_or_pay_fraction` enters only the cost ledger, so fill is bit-identical across it while cost rises strictly, and a cost-minimising optimizer would have deleted the instrument that pays for the reserved line from S11 and S13 (MD-24). It is out of both search blocks and swept as a labelled contract scenario instead, writing `contract_scenarios.csv`. And `exercise_due` requires `active_until_batches <= 0`, so a banked campaign batch blocks an exercise batch one for one (MD-23): the S16 exercise ablation moves fill +0.00163, inside its own MCSE of 0.0036, while the same ablation on S17, whose lines are rarely activated, moves +0.01070 with p_meet 0.92 to 0.48. S16's readiness claim therefore has no run behind it on this engine and its notes say so.

Two blocker re-runs were performed and recorded as provisional in the falsification register. S10 against its internal single-host control at matched capacity (the sibling its test named is configured nowhere): 0.95507 fill (MCSE 0.00755) at 18.61M USD/yr for two hosts against 0.95353 (MCSE 0.00809) at 17.92M for one, p_meet 0.16 in both arms, so the second host buys nothing measurable and costs 0.69M/yr more. S19's three activation leads with `cc_quality` present: fill 0.79513 and p_meet 0.00 at 386 d, 0.83225 and 0.00 at 201 d, 0.97695 and 0.44 at 51 d, so the switching package is worth nothing at the GDUFA III clock and something only at the CBE-30 lead an approved comparability protocol would buy, which is exactly what gate G07 leaves UNCERTAIN. Both are n = 25 without a manifest and must be re-run inside the Phase D battery.

Documentation corrections in the same pass: two dangling source ids (`LCDM-S07`, `LCDM-S02`, which are unpadded in the landscape note); the Emergent capacity-reservation attribution, where the cited task order is the CIADM one and the CIADM site is not the Camden merchant fill-finish site that was sold, corrected in `novel_architectures.md` and in `landscape/cdmos.md`; a Phlow profitability note in `morphological_matrix.csv` given its actual source and its forward-looking qualifier restored; pre-R004 vintage labels on eight run-derived matrix cells and on the Phase A cost shares carried into `novel_architectures.md`; a point-of-use marker on every unregistered scoping number; a banner row in the matrix, which travels standalone; gate G16 for the two preconditions S19 recorded as having no gate; and the Leiters row in the falsification register restated as indirect counter-evidence with the caveat its own source carries. A source-id existence check over the four artifacts plus both configs now returns zero dangling ids out of 359 citations. `make check` green: ruff, mypy --strict, 228 tests passing. `[agent-assisted; verification pass, revision R007]`


**Ranking defects fixed (2026-09-05, revisions R009 to R011).** Six defects that decided the architecture ranking were fixed together, because each one distorted a comparison the synthesis rests on. The largest was NEW-1: opening inventory was free and its quantity scaled with the design's own stock policy, so on sodium bicarbonate a deep-stock design opened with roughly five years of the network's structural capacity gap inside a five-year window and paid nothing for it. It is now charged once at the production unit value. Second was the search-space asymmetry (MD-3, MD-12): the frozen comparators searched safety stock to 90 or 120 days with no daily base-stock review while every design-space strategy searched to 365 days with it, so a cross-architecture comparison measured the search box. Every strategy now searches the same inventory space. Third, MD-1: both the order-up-to level and the opening component position scaled with the lead time, so a supplier promising faster resupply left the site holding less; pipeline cover and policy cover are now separate. Two residuals are recorded rather than argued away. A shorter lead can still show marginally more stockouts under a forced outage, because a deferred order lands when the supplier resumes whatever its nominal lead, so a longer lead carries a larger protective pipeline; that is a property of the disruption model and not of the policy. And with identical regions three of the four allocation policies remain one function, which is arithmetic: the plumbing now permits a difference and the default asserts none, because no evidence in this package says regions differ in criticality. One existing test had asserted the MD-1 defect as intended behaviour and was rewritten to the fixed invariant, which is a correction on the merits and is called out here so it is not mistaken for a weakened assertion.


**Sterilization-route screen, and its audit (2026-09-06). [agent-assisted]** `docs/route_screen/` holds a screen built to answer HA-34
from public sources: `method.md` (written before evidence gathering), four evidence stages, `route_determinations.csv` (13 rows) and
`route_screen_results.md`. The question was whether the US public record carries the sterilization route per marketed presentation.
It does not. Across 65 manufacturer labels and 50 applications behind the six `config/longlist.yaml` candidates, zero state a
sterilization method, and the FDA document that does carry it, the "Method of Sterilization:" field of a Product Quality Review,
is published with the value redacted under FOIA exemption 4. The count of unknowns is the result: 5 of 7 in-scope rows are
`unknown`, and for the two product configs the model runs on, sodium bicarbonate and norepinephrine, nothing was determined at any
confidence (0 of 24 labels, 0 of 18 applications).

The same day, an audit of that pass returned 40 findings and every blocker and major was fixed in place. What changed, because
several of the corrections cut against the pass's own conclusions: the one terminal determination on the longlist, furosemide under
ANDA 202747, was **downgraded from `strongly_inferred` to `weakly_inferred` with a `contradicted` flag**, for two reasons. Its three
"independent lines" were one registration lead (now tier E7 in `method.md`, ceiling `unknown`) plus two site-scope lines sharing one
unverified bridging premise. And the Form FDA 483 behind the warning letter the pass had cited, which the pass never opened, records
two US-market filling lines at that site and describes one of them in terms of its "aseptic gowning areas". The screen's only
`established` parametric-release instance was re-dated from 2023 to 2014 and downgraded to `strongly_inferred`, because a Dear Health
Care Provider letter for a temporary importation is not approved labelling. Four fields `method.md` required and the register omitted
(`application_holder`, `application_number`, `document_date`, `contradicted`) were added and populated; without `document_date` the
currency rule could not be applied to any row, which is how the 2023 date error survived. Three quantitative claims were corrected
against their own cited sources: an openFDA negative-hit count (eight, not reproducible; the exact phrase returns 4 and the looser
one 12), an S18 snapshot row count (916, not reproducible; 520 rows against 1,061 reported), and the ablation figure carried as the
screen's counterweight (13 of 16 is the pre-R004 run; `abl_post_R008` gives 11 of 16, and every ablation figure quoted is superseded
for quantitative use pending re-run). A claim that Drugs@FDA holds no `Review`-type document for any candidate application was false
on the frozen snapshot cited for it: three exist, all three were fetched, none carries a route statement, so the conclusion stands on
the documents rather than on their absence. Six falsification rows were appended to
`docs/design_space/falsification_register.csv` as `F11-ROUTE-01` to `-06`.

The recommendation is unchanged and better supported: do not build a route dataset yet. Three arguments were added to the case
against it, and they are the ones that matter. A validated rapid microbiological method reaches the same 9-day base pole as terminal
plus parametric release, with no supplement, no autoclave and no reformulation, and `~/telo/regulatory/release-constraints.md` calls
it a buy-and-validate. Route conversion and parametric release are both prior approval supplements filed by an application holder,
and Telo holds no application, so a confirmed route on a competitor's presentation is an observation and not an option. And every
input to the proposed pipeline is a free public register, so the dataset can be built and cannot be owned.
