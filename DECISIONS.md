# Decisions

Engineering and research decisions with rationale and reversibility. Protocol-level decisions are also logged in `protocol/revisions.csv`. Items marked *founder sign-off pending* were made conservatively by the agent-assisted workflow and should be confirmed or reversed by the user.

| ID | Date | Decision | Rationale | Reversible | Status |
|---|---|---|---|---|---|
| D001 | 2026-09-01 | Package lives at `~/telo/feasibility/` as a self-contained subproject | Task requires the existing Telo repository; root already holds `docs/`, `research/`, `regulatory/`, `paper/` and a research `pyproject.toml`; `os/quality-intelligence/` sets the subproject precedent; the empty launch directory sits in a home-dir git root with no remote | Yes (move directory) | founder sign-off pending |
| D002 | 2026-09-01 | Python floor 3.11, lock on 3.12; `uv` for environment | Protocol asks for 3.11+; repo research env is 3.12; uv gives a reproducible lock | Yes | active |
| D003 | 2026-09-01 | License Apache-2.0 for the feasibility package | Protocol package list includes LICENSE; permissive with patent grant suits a publish-the-model strategy; root repo has no license yet | Yes | founder sign-off pending |
| D004 | 2026-09-01 | CLI on `argparse`; no typer/click | Fewer dependencies; transparent | Yes | active |
| D005 | 2026-09-01 | Pre-specified q = 0.90 tail confidence and 30-day recovery window | Protocol names both but gives no number; chosen before any result existed; sensitivity 14 days | Yes via revisions.csv (decision-relevant) | founder sign-off pending |
| D006 | 2026-09-01 | Regulatory gates = 15-row workbook superset of protocol 4.4, all UNCERTAIN | Superset preserves every workbook check; UNCERTAIN never treated as PASS | Yes | active |
| D007 | 2026-09-01 | Custom daily discrete-time engine in plain Python + numpy; no SimPy/framework | Protocol prefers a transparent testable engine; entity counts are small | Yes | active |
| D008 | 2026-09-01 | Common random numbers via entity-id-keyed `SeedSequence` substreams over a universal entity roster | Strategies with different site counts still share one exogenous world; adding an entity never perturbs another | Yes | active |
| D009 | 2026-09-01 | Deterministic module keeps an exact workbook replica and a separately labeled corrected variant | Reconciliation must compare like with like; corrections must be visible, not silent | Yes | active |
| D010 | 2026-09-01 | Optimization = sample-average approximation on a CRN batch: auditable coarse grid, local refinement, full-N re-evaluation | Auditable and verifiable against the grid; no black-box solver dependency | Yes | active |
| D011 | 2026-09-01 | Large raw archives (zip/parquet) are not committed; a manifest with URL + sha256 next to each snapshot is | Repo hygiene; re-fetch is verifiable | Yes | active |
| D012 | 2026-09-01 | Product candidates stay provisional; final selection waits for frozen official snapshots and gate evidence | Protocol 2.2 step 4 and task rule "do not finalize because listed" | n/a | active |
| D013 | 2026-09-01 | Do not import `os/quality-intelligence` (telo-quality 1.3.0); adopt its four-state outcome vocabulary (CONFORMS / DOES_NOT_CONFORM / INDETERMINATE / OUT_OF_DOMAIN, "declines to certify") and re-implement the few closed forms the submodel needs, citing `conformal.py` sha256 cfe40c92...0f431 | Package is proprietary, untracked (no commit to cite), and its statistical primitives are outside its supported surface; importing GMP record machinery into a feasibility model would read as claims about the study | Yes | active |
| D014 | 2026-09-01 | Freeze the author's protocol DOCX/PDF under `protocol/source/` and the workbook under `data/raw_snapshots/workbook_scaffold/2026-09-01/` | Reconciliation tests and hash verification must not depend on `~/Downloads` | Yes | active |
| D015 | 2026-09-01 | Deterministic replica uses the workbook's typed CRF (0.16275) as an input so reconciliation is exact; the corrected variant computes CRF from rate and life | WB-09; D009 | Yes | active |
| D016 | 2026-09-02 | Design-space strategies S8+ are declared in `config/strategies/design_space.yaml` (site plans, extra suppliers, design space, profile) and built by the same runtime builder; S0-S7 stay code-defined and are guarded by `tests/regression/test_frozen_strategies.py` | Assignment rule: new comparators get new ids, never overwrite S0-S7; a declarative topology keeps every new architecture auditable and testable without another if/elif chain | Yes | active |
| D017 | 2026-09-02 | Raw-material stock carries inventory carrying cost (R003, MD-1); frozen cost snapshot re-captured with the pre-R003 value retained | The free buffer distorted the optimizer's material_target_days and the api_lead_time ablation; events are unchanged so common random numbers and every service metric stay comparable | Yes via revisions.csv | founder sign-off pending |
| D018 | 2026-09-02 | Founder's blanket permission (2026-09-02, "do whatever is necessary") is read as HA-05 commit authorization inside `feasibility/` only, plus authority to proceed under stated assumptions; it is not read as a history rewrite, a push, a publication, or a regulatory or evidence decision | The assignment forbids those without explicit approval and none of them is reversible | n/a | active |
| D019 | 2026-09-07 | A person is either an interviewee or an independent reviewer, never both. Where one candidate is the strongest name on both lists, the reviewer track wins and the interview slot goes to the next candidate for that role | `protocol/protocol.yaml` `independent_review` gives each reviewer the power to reject a class of assumption. A reviewer who supplied one of those assumptions in an interview is rejecting their own input, which is not independence. Three wave-one interview candidates were flagged as reviewer candidates and one was withdrawn from the interview wave on this rule | No | active |
| D020 | 2026-09-07 | The 8-to-12 page target for the technical memo is dropped. The memo runs to the length its argument needs; the one-page executive summary stays the short entry point | No external body enforces the page count, and the first drafting pass had already cut roughly 16 kB of section text chasing it while still landing at 22 pages, so the cut removed content and bought nothing. The two application essays keep their 400-word and 200-word limits, which the application form does enforce | No | active |
| D021 | 2026-09-07 | Outreach body length is advisory, not a gate. The hard checks on a draft are sentence length, relationship language, and model output leaking into a first contact | Published cold-email data favours 50 to 125 words, and these drafts run about 240 because the study's own disclosure rules force content in: affiliation in the first sentence, the attribution guardrail, and the stated ask size. That floor sits above the response-rate optimum, and the trade is recorded rather than hidden | No | active |
